# 技术面分析
**任务目标**：基于当前市场客观量价数据与技术指标，刻画趋势、波动、量价配合及关键价格区间，提出可回测、可证伪的量化策略思路，生成专业结构化的技术面分析报告。
**适用场景**: 对上市公司进行技术面分析。
**输出文件**：`final-output-analysis-technicals-{stock_code}.md`（由工作流 C 或工作流 E 调用）

## 核心指令
- 必须基于当前客观图表信号进行逻辑推演，绝对禁止主观臆测。
- 技术指标值必须通过skill拉取数据，严禁使用先验知识。
- 涉及两个价格偏离度计算时，遵循公式：`abs(PriceA - PriceB) / PriceB`。
- 所有数学表达式与金融计算，强制调用 math_calc 工具/技能执行，严禁模型自行幻觉心算。
- 要求启用 `数据图表渲染机制`。
- 所有“量化策略思路”均为研究假设，必须给出验证方法与局限性；不得输出面向具体用户的买卖点、止盈止损、仓位比例或其他执行指令。

## 数据补充
除了提示词上下文中提供的信息，执行前须并发补充以下数据：
- 调取 hedgehog-company-index-data 技能，查询该股票近 200 个交易日的 OHLCV 日线行情数据（**必须查询前复权数据**）。
- 调取 hedgehog-tech-indicator 技能，获取该股票的 SMA、EMA、RSI、MACD、BOLL、OBV、KDJ、ATR 等技术指标的最新及历史序列数据（200个交易日数据）。

## 输出模板
请严格按以下模块化结构输出（注意替换方括号中的指令为实际计算结果与分析内容，并渲染对应图表）：

```markdown
### 1、当前量价走势 [<150字]

[计算近5日的价格重心点：`(open + high + low + close) / 4`。]
[精准描述最新价格与 10日前、30日前、60日前价格的涨跌幅差异。]
[描述近5日价格重心的变化趋势及近5日成交量(Vol)的变化趋势，冷峻指出二者是处于趋势共振还是量价背离状态。]
[以折线图展示 200 个交易日的收盘价]

### 2、技术指标分析 

#### 趋势型指标分析 [<100字]
[重点分析 SMA 和 MACD 指标当前形态（如金叉/死叉/均线排列），推演出“正向”、“负向”或“中性”的方向性结论。]

#### 震荡/动量型指标分析 [<100字]
[重点分析 RSI 和 KDJ 指标的超买超卖状态及背离情况，推演出“正向”、“负向”或“中性”的方向性结论。]

#### 波动/通道型指标分析 [<100字]
[重点分析 BOLL 轨道宽窄与价格位置，及 ATR 波动率变化，推演出“正向”、“负向”或“中性”的方向性结论。]

#### 量价型指标 [<100字]
[重点分析 OBV 能量潮与价格前高的配合度，推演出“正向”、“负向”或“中性”的方向性结论。]

#### 指标分析汇总 [<200字]
[以表格形式汇总上述四大维度的指标值、观察窗口及方向性结论。]

### 3、支撑阻力位与价格空间分析

#### 支撑和阻力位 [<200字]
[提取近30个交易日的最高价（High）与最低价（Low），提取最新 ATR 值与最新收盘价。]
[**阻力位计算**：计算 `近30日最高价`、`最新价 + 2*ATR`、`最新价 * 1.618`。过滤规则：若最低阻力位与最新价相差 5% 以内则剔除；若两个阻力位相差 5% 以内则剔除较小者。列出最终有效阻力位。]
[**支撑位计算**：计算 `近30日最低价`、`最新价 - 1.5*ATR`、`最新价 * 0.618`。过滤规则：若最高支撑位与最新价相差 5% 以内则剔除；若两个支撑位相差 5% 以内则剔除较小者。列出最终有效支撑位。]

#### 价格空间不对称度 [<120字]
[取最小有效阻力位为 ResistancePrice，最大有效支撑位为 SupportPrice，当前收盘价为 Price。]
[计算上方距离 `UpDistance = (ResistancePrice - Price) / Price`、下方距离 `DownDistance = (Price - SupportPrice) / Price`，以及空间不对称度 `Asymmetry = UpDistance / DownDistance`。]
[仅描述当前价格在支撑—阻力区间中的相对位置和上下空间是否对称，不把阈值解释为买卖建议。若有效价位缺失或分母不为正，明确标注该指标不可计算。]

### 4、量化策略思路 [<200字]

[综合上述趋势、支撑阻力、波动与量价特征，将观察结果重构为研究用途的量化假设，不得直接转化为交易执行建议：]
- 研究假设：[用“可观测条件 → 待检验结果”表述。例如：当日线收盘价高于阻力位且成交量位于近60日80%分位以上时，未来5/10/20日相对基准收益是否显著为正。不得将该条件称为入场点。]
- 信号定义：[明确指标公式、阈值、观察窗口、状态编码与数据频率；阈值必须来自本次数据或明确的统计分位数，不得事后调整。]
- 回测设计：[说明样本区间、基准、持有观察窗口、复权方式、交易成本假设、训练/验证划分及避免未来函数的方法；现有数据不足时明确需要补充的样本。]
- 评价与失效：[至少列出信号覆盖率、胜率、平均超额收益、最大回撤和稳健性检验，并说明结构突变、样本偏差、参数敏感或统计不显著等失效情形。未完成回测时必须标注“待验证”，不得宣称策略有效。]

---

（注：技术分析由AI推演生成，仅供研究与学习参考，绝不构成投资建议。）

### [参考资料]
[汇总所有 Sub-agent 收集的资料，严格按 `尾注规范` 格式排列]

### [图表数据]
[严格按 `尾注规范` 格式排列]

### [AI生成提示]
以上内容由AI生成，包含技术分析与模型推演，仅供参考与学习，绝不构成投资建议。
[附加说明：如先验知识使用声明、数据源缺失留白声明、委员会审查记录, 严格按 `尾注规范` 格式排列]
